/* ============================================================
   Versione del gestionale
   ------------------------------------------------------------
   Un solo punto da aggiornare: da qui la versione compare nella
   barra laterale, nelle impostazioni, nei backup e nei documenti
   stampati. Il numero segue la forma MAIUSCOLA.MINORE:
     - la cifra minore cambia con le funzioni nuove e le correzioni,
       che si aggiungono senza toccare il lavoro gia' fatto;
     - la cifra maiuscola cambia quando serve attenzione da parte di
       chi lo usa: qualcosa si fa in modo diverso, o i dati esistenti
       vanno convertiti.
   Lo storico delle versioni sta in docs/versioni.md.
   ============================================================ */
export const VERSIONE = '1.1';
export const VERSIONE_DATA = '2026-09-21';
